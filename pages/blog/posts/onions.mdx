---
title: 'Next.js page options and how they work'
metaTitle: 'Next.js page options and how they work'
metaDesc: 'How to use pages in Next.js exploring the options'
socialImage: images/22-09-2021.jpg
date: '2021-09-22'
tags:
  - NextJS
  - ReactJS
---

import Button from '@mui/material/Button';
import { Typography} from "@mui/material";

### Onions

Layers. Onions have layers. Ogres have layers. Onions have layers. You get it? We both have layers.Layers. Onions have layers. Ogres have layers. Onions have layers. 
You get it? We both have layers.Layers. Onions have layers. Ogres have layers. Onions have layers. You get it? We both have layers.

Oh, you both have layers. Oh. You know, not everybody like onions.

```js
const year = "2001";
const movie = "Shrek";
console.log(`${movie}, ${year}`);
```
That was a code snippet.
Pretty cool right? Syntax highlighting with prism.
```css
.box{
    padding:0px;
}
```

Let's test a list 
- The first 
- second
- third
    - sublist?

Lets test out **BOLD**

Yep looks good 

Layers. Onions have layers. Ogres have layers. Onions have layers. You get it? We both have layers.Layers. Onions have layers. Ogres have layers. Onions have layers. 
You get it? We both have layers.Layers. Onions have layers. Ogres have layers. Onions have layers. You get it? We both have layers.

Oh, you both have layers. Oh. You know, not everybody like onions.

```js
const year = "2001";
const movie = "Shrek";
console.log(`${movie}, ${year}`);
```
That was a code snippet.
Pretty cool right? Syntax highlighting with prism.
```css
.box{
    padding:0px;
}
```

Let's test a list 
- The first 
- second
- third
    - sublist?

Lets test out **BOLD**

Yep looks good 
Layers. Onions have layers. Ogres have layers. Onions have layers. You get it? We both have layers.Layers. Onions have layers. Ogres have layers. Onions have layers. 
You get it? We both have layers.Layers. Onions have layers. Ogres have layers. Onions have layers. You get it? We both have layers.

Oh, you both have layers. Oh. You know, not everybody like onions.

```js
const year = "2001";
const movie = "Shrek";
console.log(`${movie}, ${year}`);
```
That was a code snippet.
Pretty cool right? Syntax highlighting with prism.
```css
.box{
    padding:0px;
}
```

Let's test a list 
- The first 
- second
- third
    - sublist?

Lets test out **BOLD**

Yep looks good 