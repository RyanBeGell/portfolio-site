import Button from '@mui/material/Button';
import { Typography} from "@mui/material";
import Header from '@/src/components/blog/Header/Header'

<Header 
    date="Jan 18, 2023"
    minRead="2"
    />

# DevBlog - This is what a title in my dev blog is going to look like [Updated 2023]
Layers. Onions have layers. Ogres have layers. Onions have layers. You get it? We both have layers.Layers. Onions have layers. Ogres have layers. Onions have layers. 
You get it? We both have layers.Layers. Onions have layers. Ogres have layers. Onions have layers. You get it? We both have layers.

Oh, you both have layers. Oh. You know, not everybody like onions.

```js
const year = "2001";
const movie = "Shrek";
console.log(`${movie}, ${year}`);
```

That was a code snippet.
Pretty cool right? Syntax highlighting with prism.

```css
.box{
    padding:0px;
}
```

Let's test a list 
- The first 
- second
- third
    - sublist?

Lets test out **BOLD**

Yep looks good 

Layers. Onions have layers. Ogres have layers. Onions have layers. You get it? We both have layers.Layers. Onions have layers. Ogres have layers. Onions have layers. 
You get it? We both have layers.Layers. Onions have layers. Ogres have layers. Onions have layers. You get it? We both have layers.

Oh, you both have layers. Oh. You know, not everybody like onions.

```js
const year = "2001";
const movie = "Shrek";
console.log(`${movie}, ${year}`);
```
That was a code snippet.
Pretty cool right? Syntax highlighting with prism.

```css
.box{
    padding:0px;
}
```

Let's test a list 
- The first 
- second
- third
    - sublist?

Lets test out **BOLD**

Yep looks good 
Layers. Onions have layers. Ogres have layers. Onions have layers. You get it? We both have layers.Layers. Onions have layers. Ogres have layers. Onions have layers. 
You get it? We both have layers.Layers. Onions have layers. Ogres have layers. Onions have layers. You get it? We both have layers.

Oh, you both have layers. Oh. You know, not everybody like onions.

```js
const year = "2001";
const movie = "Shrek";
console.log(`${movie}, ${year}`);
```
That was a code snippet.
Pretty cool right? Syntax highlighting with prism.

```css
.box{
    padding:0px;
}
```

```java
public static void main(String[] args){

}
```
Let's test a list 
- The first 
- second
- third
    - sublist?

Lets test out **BOLD**
Yep looks good